# Chrome Extension Project

이 프로젝트는 **React**, **TypeScript**, **Vite**를 기반으로 한 **모듈형 Chrome 확장프로그램**입니다.
다양한 기능을 독립적인 모듈로 관리하며, 기능별로 사이트 매칭 및 활성화 여부를 제어할 수 있는 아키텍처를 가지고 있습니다.

## 시작하기

### 필수 요구사항
- Node.js
- pnpm

### 설치

의존성을 설치합니다:

```bash
pnpm install
```

### 개발 및 빌드

개발 서버 실행 (HMR):
```bash
pnpm dev
```

프로덕션 빌드:
```bash
pnpm build
```

빌드가 완료되면 `dist` 폴더가 생성됩니다.

### Chrome에 로드하기

1. Chrome 브라우저 주소창에 `chrome://extensions` 입력.
2. 우측 상단 **개발자 모드** 활성화.
3. **압축해제된 확장 프로그램을 로드합니다** 클릭.
4. 프로젝트의 `dist` 폴더 선택.

## ✨ 프로젝트 특징 (모듈형 아키텍처)

이 프로젝트는 `src/features` 디렉토리 하위에 기능들을 모듈 단위로 관리합니다.

- **독립적 기능**: 각 기능은 독립된 폴더와 파일(`index.ts`)을 가집니다.
- **조건부 실행**: URL 패턴(`matches`)에 따라 자동으로 실행 여부가 결정됩니다.
- **설정 중앙화**: 팝업 UI에서 각 기능을 켜고 끌 수 있는 UI가 자동으로 생성됩니다.

## 📂 프로젝트 구조

```
src/
├── features/             # 기능 모듈들이 위치하는 곳
│   ├── dummy-notice/     # [예시] 더미 알림 기능
│   └── registry.ts       # 기능 등록 파일 (새 기능 추가 시 여기에 등록)
├── utils/
│   ├── storage.ts        # 기능 On/Off 상태 저장소
│   └── url-matcher.ts    # URL 패턴 매칭 유틸리티
├── content/              # 콘텐츠 스크립트 (기능 로더 역할)
├── popup/                # 팝업 UI (기능 설정 패널)
└── background/           # 백그라운드 스크립트
```

## 🛠 새로운 기능 추가하기

1. `src/features/` 아래에 새 폴더를 생성합니다 (예: `my-feature`).
2. `index.ts`를 생성하고 `Feature` 인터페이스를 구현합니다.

   ```typescript
   export const myFeature: Feature = {
     id: 'my-feature',
     name: '내 멋진 기능',
     description: '이 기능에 대한 설명',
     matches: ['example.com'], // 실행될 사이트
     initialState: true,
     execute: () => {
       console.log('기능 실행!');
     }
   };
   ```

3. `src/features/registry.ts` 배열에 추가하면 끝!
